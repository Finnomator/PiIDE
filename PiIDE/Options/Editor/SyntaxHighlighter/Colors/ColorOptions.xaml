<UserControl x:Class="PiIDE.Options.Editor.SyntaxHighlighter.Colors.ColorOptions"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:PiIDE.Options.Editor.SyntaxHighlighter.Colors"
             mc:Ignorable="d" d:DesignWidth="800" Unloaded="UserControl_Unloaded">

    <StackPanel>
        <StackPanel Name="MainStackPanel"/>
        <Separator Background="{StaticResource SplitterBackground}"/>

        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition/>
                <ColumnDefinition/>
            </Grid.ColumnDefinitions>

            <Button Style="{StaticResource CleanButtonStyle}" Content="Export Theme" Foreground="White" Click="ExportTheme_Click"/>
            <Button Style="{StaticResource CleanButtonStyle}" Content="Import Theme" Foreground="White" Grid.Column="1" Click="ImportTheme_Click"/>
        </Grid>
    </StackPanel>
</UserControl>
