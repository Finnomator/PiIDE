<UserControl x:Class="PiIDE.TextEditor"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:PiIDE"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800" PreviewKeyDown="TextEditor_PreviewKeyDown">

    <Grid>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="5"/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        
        
        <ListBox x:Name="LineNumsListBox" VirtualizingStackPanel.VirtualizationMode="Recycling" FontFamily="Cascadia Code"/>
        <GridSplitter Grid.Column="1" HorizontalAlignment="Stretch"/>
        <ListBox Grid.Column="2" x:Name="TextEditorListBox" VirtualizingStackPanel.VirtualizationMode="Recycling" IsHitTestVisible="False" Background="{x:Null}"/>
        <TextBox x:Name="TextEditorTextBox" Grid.Column="2" TextChanged="TextEditorTextBox_TextChanged" FontFamily="Cascadia Code" AcceptsTab="True" AcceptsReturn="True"/>
        <Canvas Grid.Column="2" x:Name="CompletionsContainerCanvas">
            <local:CompletionUiList x:Name="CompletionUiList"/>
        </Canvas>
    </Grid>
</UserControl>
