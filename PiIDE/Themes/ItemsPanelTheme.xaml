<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Style x:Key="GridViewColumnHeaderStyle" TargetType="{x:Type GridViewColumnHeader}">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type GridViewColumnHeader}">
                    <!--
                    <Border BorderThickness="0,0,0,2" BorderBrush="{DynamicResource SplitterBackgroundBrush}" Background="Transparent">
                        <Label x:Name="ContentHeader" Content="{TemplateBinding Content}"  Width="{TemplateBinding Width}" Foreground="White"/>
                    </Border>
                    -->
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
</ResourceDictionary>