<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <ControlTemplate x:Key="GridViewColumnHeaderStyle" TargetType="GridViewColumnHeader">
        <Border BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="0,1,0,1">
            <Grid Background="{TemplateBinding Background}">
                <TextBlock Background="{TemplateBinding Background}" Foreground="{TemplateBinding Foreground}" Text="{TemplateBinding Tag}" VerticalAlignment="Center" HorizontalAlignment="Center"/>
                <GridSplitter x:Name="PART_HeaderGripper" HorizontalAlignment="Right" Width="2" Background="{StaticResource SplitterBackgroundBrush}"/>
            </Grid>
        </Border>
    </ControlTemplate>
    <Style TargetType="{x:Type GridViewColumnHeader}">
        <Setter Property="Foreground" Value="White"/>
        <Setter Property="BorderBrush" Value="{StaticResource SplitterBackgroundBrush}"/>
        <Setter Property="Background" Value="{StaticResource PanelBackgroundBrush}"/>
    </Style>
</ResourceDictionary>